package org.example;

public class Interest {
    //        A method to allow the account to pay interest:
    public double updatedBalance(double balance, double interestAdded) {

        { return balance += balance * interestAdded;
        }

    }
}
